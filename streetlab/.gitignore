node_modules/
dist/
.DS_Store
*.local

# Claude Code session state. `.claude/worktrees/` in particular holds nested git
# worktrees of this same repository — committing them would recurse.
.claude/

# Rust / Tauri
src-tauri/target/
src-tauri/gen/
# The sidecar binary is a build output (scripts/build_app.sh), not source.
src-tauri/bin/

# Test artifacts
test-results/
playwright-report/
blob-report/
.playwright/
